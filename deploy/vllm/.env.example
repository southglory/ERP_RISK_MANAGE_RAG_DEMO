# Copy to .env and fill in
HF_TOKEN=hf_xxxxxxxxxxxx
# GPU_UTIL=0.55 when sharing GPU with infinity (default)
# GPU_UTIL=0.85 when vLLM alone on GPU
GPU_UTIL=0.55
# HF_CACHE=C:/Users/yourname/.cache/huggingface  # use host cache to avoid re-download
